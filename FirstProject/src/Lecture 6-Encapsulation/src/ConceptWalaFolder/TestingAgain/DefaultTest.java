package ConceptWalaFolder.TestingAgain;

public class DefaultTest {
    public static void main(String[] args) {
        // Simply saying we can't access other package's default method or instance variable(Access within package)
        // Public(Anywhere)
        // Private(Inside the class)
    }
}
