package in.Abstra.Vehicle;

public interface Transport { // interface is parent of parent class, it is only made to declare abstract methods for classes
    // multiple interfaces can be inherited , but not classes

    void getSetGo(); // we don't have to write public abstract void getSetGo() as it already is
}
